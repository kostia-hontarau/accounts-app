export default class Account {
  constructor({ name, surname, email, city, phone, postalCode }) {
    this.name = name;
    this.surname = surname;
    this.email = email;
    this.city = city;
    this.phone = phone;
    this.postalCode = postalCode;
  }
}
